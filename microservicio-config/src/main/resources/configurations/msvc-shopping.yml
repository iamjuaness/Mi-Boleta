server:
  port: 8088

spring:
  application:
    name: msvc-shopping

  eureka:
    instance:
      hostname: localhost
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka
